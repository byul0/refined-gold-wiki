<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Wiki Refined Gold Overhaul</title>
  <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  <style>
    :root { --bg: #0f1117; --sidebar: #161822; --card: #1c1f2e; --accent: #f0c040; --text: #e4e6f0; --border: #2a2d3e; }
    body { margin: 0; background: var(--bg); color: var(--text); font-family: 'Segoe UI', sans-serif; display: flex; }
    #sidebar { width: 260px; background: var(--sidebar); height: 100vh; overflow-y: auto; border-right: 1px solid var(--border); position: fixed; }
    .nav-item { padding: 12px 20px; cursor: pointer; border-bottom: 1px solid var(--border); font-size: 14px; transition: 0.2s; color: #8b8fa3; }
    .nav-item:hover { background: var(--card); color: white; }
    .nav-item.active { background: rgba(240,192,64,0.1); color: var(--accent); border-left: 4px solid var(--accent); font-weight: bold; }
    #content { margin-left: 260px; padding: 40px; width: calc(100% - 260px); box-sizing: border-box; }
    table { width: 100%; border-collapse: collapse; background: var(--card); border-radius: 8px; overflow: hidden; margin-bottom: 30px; border: 1px solid var(--border); }
    th { text-align: left; padding: 12px 15px; color: #8b8fa3; font-size: 11px; text-transform: uppercase; background: rgba(0,0,0,0.2); }
    td { padding: 10px 15px; border-bottom: 1px solid var(--border); }
    .pkm-row { display: flex; align-items: center; gap: 15px; }
    .sprite-box { width: 50px; height: 50px; display: flex; align-items: center; justify-content: center; background: rgba(255,255,255,0.03); border-radius: 6px; }
    .sprite-box img { max-width: 45px; max-height: 45px; }
    .rate { color: var(--accent); font-weight: bold; font-family: monospace; }
  </style>
</head>
<body>
  <div id="root"></div>

  <script type="text/babel">
    const { useState } = React;

    // Tes données intégrées ici
    const ENCOUNTER_DATA = {
      "Route 29": {
        walking: [
          { pokemon: "Pidgey", rate: "50%", time: "Morning, Day" },
          { pokemon: "Sentret", rate: "40%", time: "Morning, Day" },
          { pokemon: "Hoppip", rate: "5%", time: "Morning, Day" },
          { pokemon: "Rattata", rate: "5%", time: "Morning, Day" },
          { pokemon: "Hoothoot", rate: "60%", time: "Night" },
          { pokemon: "Rattata", rate: "35%", time: "Night" },
          { pokemon: "Sentret", rate: "5%", time: "Night" },
        ]
      },
      "Route 30": {
        walking: [
          { pokemon: "Caterpie", rate: "20%", time: "Morning, Day" },
          { pokemon: "Weedle", rate: "20%", time: "Morning, Day" },
          { pokemon: "Hoppip", rate: "10%", time: "Morning, Day" },
          { pokemon: "Ledyba", rate: "25%", time: "Morning" },
          { pokemon: "Hoothoot", rate: "30%", time: "Night" },
        ]
      },
      "Route 31": {
        walking: [
          { pokemon: "Bellsprout", rate: "20%", time: "All" },
          { pokemon: "Caterpie", rate: "20%", time: "Morning, Day" },
          { pokemon: "Weedle", rate: "15%", time: "Morning, Day" },
        ]
      },
      "Ilex Forest": {
        walking: [
          { pokemon: "Butterfree", rate: "4%", time: "All" },
          { pokemon: "Beedrill", rate: "4%", time: "All" },
          { pokemon: "Paras", rate: "15%", time: "Morning" },
        ]
      },
      "Ruins of Alph": {
        walking: [
          { pokemon: "Natu", rate: "70%", time: "Exterior" },
          { pokemon: "Smeargle", rate: "30%", time: "Exterior" },
          { pokemon: "Unown", rate: "100%", time: "Interior" }
        ]
      }
    };

    const getSprite = (n) => {
      const clean = n.toLowerCase().replace(/\s+/g, "").replace(/[^a-z0-9]/g, "");
      return `https://play.pokemonshowdown.com/sprites/ani/${clean}.gif`;
    };

    const Table = ({ title, icon, data }) => {
      if (!data || data.length === 0) return null;
      return (
        <div>
          <h3 style={{display:'flex', alignItems:'center', gap:'10px', color: 'var(--accent)'}}>{icon} {title}</h3>
          <table>
            <thead><tr><th>Pokémon</th><th>Taux</th><th>Moment / Condition</th></tr></thead>
            <tbody>
              {data.map((e, i) => (
                <tr key={i}>
                  <td>
                    <div className="pkm-row">
                      <div className="sprite-box">
                        <img src={getSprite(e.pokemon)} onError={(e)=>e.target.src='https://play.pokemonshowdown.com/sprites/dex/unown.png'} />
                      </div>
                      <strong>{e.pokemon}</strong>
                    </div>
                  </td>
                  <td><span className="rate">{e.rate}</span></td>
                  <td style={{fontSize:'0.9em', color:'#8b8fa3'}}>{e.time || e.method || "-"}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      );
    };

    function App() {
      const [sel, setSel] = useState(Object.keys(ENCOUNTER_DATA)[0]);
      const d = ENCOUNTER_DATA[sel];

      return (
        <div style={{display:'flex', width:'100%'}}>
          <div id="sidebar">
            <div style={{padding:'25px 20px', fontWeight:'800', color:'var(--accent)', letterSpacing:'1px'}}>WIKI REFINED GOLD</div>
            {Object.keys(ENCOUNTER_DATA).map(k => (
              <div key={k} className={`nav-item ${sel===k?'active':''}`} onClick={()=>setSel(k)}>{k}</div>
            ))}
          </div>
          <div id="content">
            <h1 style={{fontSize:'2.5em', margin:'0 0 40px 0'}}>{sel}</h1>
            <Table title="Rencontres" icon="🌿" data={d.walking} />
          </div>
        </div>
      );
    }

    const root = ReactDOM.createRoot(document.getElementById('root'));
    root.render(<App />);
  </script>
</body>
</html>
